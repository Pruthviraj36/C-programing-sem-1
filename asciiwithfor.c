#include<stdio.h>
  
     void main(){
     	int a,b,n,c;
     	scanf("%d",&n);
     	if(n==97){
		 
     	for(a=97;a<=122;a++){
     		printf("%c =%d\n",a,a);
     	}
     }
	     scanf("%d",&c);
		 if(c==65){
		 
		 for(b=65;b<=90;b++){
		   printf("%c = %d\n",b,b);
      }
    }
     
 }
